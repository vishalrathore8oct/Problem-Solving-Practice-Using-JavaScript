1. **Print Numbers from 1 to N**
   - Problem Statement:

     Write a program that takes an integer `N` as input and prints all numbers from `1` to `N` in increasing order, separated by spaces.

   - Sample Input 1:

     ```
     5
     ```

     Sample Output 1:

     ```
     1 2 3 4 5
     ```

   - Sample Input 2:

     ```
     1
     ```

     Sample Output 2:

     ```
     1
     ```

   - Sample Input 3:

     ```
     8
     ```

     Sample Output 3:

     ```
     1 2 3 4 5 6 7 8
     ```

---

2. **Print Numbers from N to 1 without Changing the Loop Condition**
   - Problem Statement:

     Write a program that takes an integer `N` as input and prints all numbers from `N` to `1` in decreasing order **without changing the loop condition used for printing numbers from `1` to `N`**.

   - Sample Input 1:

     ```
     5
     ```

     Sample Output 1:

     ```
     5 4 3 2 1
     ```

   - Sample Input 2:

     ```
     1
     ```

     Sample Output 2:

     ```
     1
     ```

   - Sample Input 3:

     ```
     8
     ```

     Sample Output 3:

     ```
     8 7 6 5 4 3 2 1
     ```

---

3. **Print All Even Numbers from 1 to N**
   - Problem Statement:

     Write a program that takes an integer `N` as input and prints all even numbers from `1` to `N`, separated by spaces.

   - Sample Input 1:

     ```
     10
     ```

     Sample Output 1:

     ```
     2 4 6 8 10
     ```

   - Sample Input 2:

     ```
     7
     ```

     Sample Output 2:

     ```
     2 4 6
     ```

   - Sample Input 3:

     ```
     2
     ```

     Sample Output 3:

     ```
     2
     ```

4. **Sum of First N Natural Numbers**
   - Problem Statement:

     Write a program that takes an integer `N` as input and prints the sum of the first `N` natural numbers.

   - Sample Input 1:

     ```
     5
     ```

     Sample Output 1:

     ```
     15
     ```

   - Sample Input 2:

     ```
     1
     ```

     Sample Output 2:

     ```
     1
     ```

   - Sample Input 3:

     ```
     10
     ```

     Sample Output 3:

     ```
     55
     ```

---

5. **Product (Factorial) of N**
   - Problem Statement:

     Write a program that takes an integer `N` as input and prints the factorial of `N`. The factorial of a number is the product of all positive integers from `1` to `N`.

   - Sample Input 1:

     ```
     5
     ```

     Sample Output 1:

     ```
     120
     ```

   - Sample Input 2:

     ```
     1
     ```

     Sample Output 2:

     ```
     1
     ```

   - Sample Input 3:

     ```
     6
     ```

     Sample Output 3:

     ```
     720
     ```

---

6. **Sum of All Even Numbers up to N**
   - Problem Statement:

     Write a program that takes an integer `N` as input and prints the sum of all even numbers from `1` to `N`.

   - Sample Input 1:

     ```
     10
     ```

     Sample Output 1:

     ```
     30
     ```

   - Sample Input 2:

     ```
     8
     ```

     Sample Output 2:

     ```
     20
     ```

   - Sample Input 3:

     ```
     1
     ```

     Sample Output 3:

     ```
     0
     ```

7. **Print Squares of Numbers from 1 to N**
   - Problem Statement:

     Write a program that takes an integer `N` as input and prints the square of each number from `1` to `N`, separated by spaces.

   - Sample Input 1:

     ```
     5
     ```

     Sample Output 1:

     ```
     1 4 9 16 25
     ```

   - Sample Input 2:

     ```
     3
     ```

     Sample Output 2:

     ```
     1 4 9
     ```

   - Sample Input 3:

     ```
     6
     ```

     Sample Output 3:

     ```
     1 4 9 16 25 36
     ```

---

8. **Print All Numbers Divisible by 3 and 5 up to N**
   - Problem Statement:

     Write a program that takes an integer `N` as input and prints all numbers from `1` to `N` that are divisible by both `3` and `5`, separated by spaces.

   - Sample Input 1:

     ```
     30
     ```

     Sample Output 1:

     ```
     15 30
     ```

   - Sample Input 2:

     ```
     20
     ```

     Sample Output 2:

     ```
     15
     ```

   - Sample Input 3:

     ```
     14
     ```

     Sample Output 3:

     ```
     No numbers found
     ```

---

9. **Find the Sum of All Odd Numbers up to N**
   - Problem Statement:

     Write a program that takes an integer `N` as input and prints the sum of all odd numbers from `1` to `N`.

   - Sample Input 1:

     ```
     10
     ```

     Sample Output 1:

     ```
     25
     ```

   - Sample Input 2:

     ```
     5
     ```

     Sample Output 2:

     ```
     9
     ```

   - Sample Input 3:

     ```
     1
     ```

     Sample Output 3:

     ```
     1
     ```

10. **Print the Cubes of Numbers from 1 to N**

- Problem Statement:

  Write a program that takes an integer `N` as input and prints the cube of each number from `1` to `N`, separated by spaces.

- Sample Input 1:

  ```
  5
  ```

  Sample Output 1:

  ```
  1 8 27 64 125
  ```

- Sample Input 2:

  ```
  3
  ```

  Sample Output 2:

  ```
  1 8 27
  ```

- Sample Input 3:

  ```
  6
  ```

  Sample Output 3:

  ```
  1 8 27 64 125 216
  ```

---

11. **Print Only the Numbers That Are Both Even and Perfect Squares**

- Problem Statement:

  Write a program that takes an integer `N` as input and prints all numbers from `1` to `N` that are both even and perfect squares, separated by spaces.

- Sample Input 1:

  ```
  20
  ```

  Sample Output 1:

  ```
  4 16
  ```

- Sample Input 2:

  ```
  50
  ```

  Sample Output 2:

  ```
  4 16 36
  ```

- Sample Input 3:

  ```
  100
  ```

  Sample Output 3:

  ```
  4 16 36 64 100
  ```

12. **Classify a Person Based on Age**

- Problem Statement:

  Write a program that takes an integer input representing a person's age and prints the corresponding age group based on the following conditions:
  - If the age is less than `13`, print `"Child"`.
  - If the age is `13` or more but less than `20`, print `"Teen"`.
  - If the age is `20` or more but less than `60`, print `"Adult"`.
  - If the age is `60` or more, print `"Senior"`.

- Sample Input 1:

  ```
  8
  ```

  Sample Output 1:

  ```
  Child
  ```

- Sample Input 2:

  ```
  17
  ```

  Sample Output 2:

  ```
  Teen
  ```

- Sample Input 3:

  ```
  65
  ```

  Sample Output 3:

  ```
  Senior
  ```

---

13. **Check Whether a Number is Positive, Negative, or Zero**

- Problem Statement:

  Write a program that takes an integer as input and prints whether the number is `"Positive"`, `"Negative"`, or `"Zero"`.

- Sample Input 1:

  ```
  15
  ```

  Sample Output 1:

  ```
  Positive
  ```

- Sample Input 2:

  ```
  -3
  ```

  Sample Output 2:

  ```
  Negative
  ```

- Sample Input 3:

  ```
  0
  ```

  Sample Output 3:

  ```
  Zero
  ```

---

14. **Find the Largest Among Three Numbers**

- Problem Statement:

  Write a program that takes three integers as input and prints the largest among them. If two or more numbers are equal and also the largest, print that number only once.

- Sample Input 1:

  ```
  4
  9
  2
  ```

  Sample Output 1:

  ```
  9
  ```

- Sample Input 2:

  ```
  7
  7
  3
  ```

  Sample Output 2:

  ```
  7
  ```

- Sample Input 3:

  ```
  12
  5
  12
  ```

  Sample Output 3:

  ```
  12
  ```

15. **FizzBuzz**

- Problem Statement:

  Write a program that takes an integer as input and prints:
  - `"FizzBuzz"` if the number is divisible by both `3` and `5`.
  - `"Fizz"` if the number is divisible only by `3`.
  - `"Buzz"` if the number is divisible only by `5`.
  - Otherwise, print the number itself.

- Sample Input 1:

  ```
  15
  ```

  Sample Output 1:

  ```
  FizzBuzz
  ```

- Sample Input 2:

  ```
  9
  ```

  Sample Output 2:

  ```
  Fizz
  ```

- Sample Input 3:

  ```
  7
  ```

  Sample Output 3:

  ```
  7
  ```

---

16. **Calculate Student Grade**

- Problem Statement:

  Write a program that takes a student's score as an integer input (`0` to `100`) and prints the corresponding grade based on the following rules:
  - `90` to `100` → `"A"`
  - `80` to `89` → `"B"`
  - `70` to `79` → `"C"`
  - `60` to `69` → `"D"`
  - Below `60` → `"F"`

- Sample Input 1:

  ```
  85
  ```

  Sample Output 1:

  ```
  B
  ```

- Sample Input 2:

  ```
  52
  ```

  Sample Output 2:

  ```
  F
  ```

- Sample Input 3:

  ```
  94
  ```

  Sample Output 3:

  ```
  A
  ```

---

17. **Check Whether a Year is a Leap Year**

- Problem Statement:

  Write a program that takes a year as input and checks whether it is a leap year.

  A year is a leap year if:
  - It is divisible by `4`, and
  - It is **not** divisible by `100`, unless it is also divisible by `400`.

  Print `"Leap Year"` if it is a leap year; otherwise, print `"Not a Leap Year"`.

- Sample Input 1:

  ```
  2020
  ```

  Sample Output 1:

  ```
  Leap Year
  ```

- Sample Input 2:

  ```
  1900
  ```

  Sample Output 2:

  ```
  Not a Leap Year
  ```

- Sample Input 3:

  ```
  2000
  ```

  Sample Output 3:

  ```
  Leap Year
  ```

18. **Check Whether a Character is a Digit**

- Problem Statement:

  Write a program that takes a single character as input and checks whether it is a digit (`0`–`9`).
  - If the character is a digit, print `"Digit"`.
  - Otherwise, print `"Not a Digit"`.

- Sample Input 1:

  ```
  5
  ```

  Sample Output 1:

  ```
  Digit
  ```

- Sample Input 2:

  ```
  a
  ```

  Sample Output 2:

  ```
  Not a Digit
  ```

- Sample Input 3:

  ```
  0
  ```

  Sample Output 3:

  ```
  Digit
  ```

---

19. **Determine the Type of Triangle**

- Problem Statement:

  Write a program that takes three positive integers as input representing the lengths of the sides of a triangle and determines its type.
  - If all three sides are equal, print `"Equilateral"`.
  - If exactly two sides are equal, print `"Isosceles"`.
  - If all three sides are different, print `"Scalene"`.

- Sample Input 1:

  ```
  5
  5
  5
  ```

  Sample Output 1:

  ```
  Equilateral
  ```

- Sample Input 2:

  ```
  4
  4
  6
  ```

  Sample Output 2:

  ```
  Isosceles
  ```

- Sample Input 3:

  ```
  3
  4
  5
  ```

  Sample Output 3:

  ```
  Scalene
  ```

---

20. **Categorize Temperature**

- Problem Statement:

  Write a program that takes an integer input representing the temperature in degrees Celsius and categorizes it as follows:
  - If the temperature is below `0`, print `"Freezing"`.
  - If the temperature is from `0` to `20` (inclusive), print `"Cold"`.
  - If the temperature is from `21` to `30` (inclusive), print `"Warm"`.
  - If the temperature is above `30`, print `"Hot"`.

- Sample Input 1:

  ```
  -5
  ```

  Sample Output 1:

  ```
  Freezing
  ```

- Sample Input 2:

  ```
  15
  ```

  Sample Output 2:

  ```
  Cold
  ```

- Sample Input 3:

  ```
  35
  ```

  Sample Output 3:

  ```
  Hot
  ```

21. **Calculate Electricity Bill**

- Problem Statement:

  Write a program that takes the number of electricity units consumed as input and calculates the total electricity bill using the following slab rates:
  - First `100` units → ₹`5` per unit
  - Next `100` units (`101`–`200`) → ₹`7` per unit
  - Next `100` units (`201`–`300`) → ₹`10` per unit
  - Above `300` units → ₹`12` per unit

  Calculate the bill cumulatively and print the total amount.

- Sample Input 1:

  ```
  80
  ```

  Sample Output 1:

  ```
  400
  ```

- Sample Input 2:

  ```
  150
  ```

  Sample Output 2:

  ```
  850
  ```

- Sample Input 3:

  ```
  230
  ```

  Sample Output 3:

  ```
  1500
  ```

---

22. **Check if a Character is a Vowel or Consonant**

- Problem Statement:

  Write a program that takes a single alphabet character as input and prints whether it is a `"Vowel"` or a `"Consonant"`.

  Consider both uppercase and lowercase vowels (`A`, `E`, `I`, `O`, `U`).

- Sample Input 1:

  ```
  e
  ```

  Sample Output 1:

  ```
  Vowel
  ```

- Sample Input 2:

  ```
  B
  ```

  Sample Output 2:

  ```
  Consonant
  ```

- Sample Input 3:

  ```
  u
  ```

  Sample Output 3:

  ```
  Vowel
  ```

---

23. **Determine the Type of Triangle (Including Right Triangle)**

- Problem Statement:

  Write a program that takes three positive integers as input representing the sides of a triangle and determines its type using the following rules:
  - If `a² + b² = c²` (or any valid combination), print `"Right-Angled Triangle"`.
  - Else if all three sides are equal, print `"Equilateral Triangle"`.
  - Else if any two sides are equal, print `"Isosceles Triangle"`.
  - Otherwise, print `"Scalene Triangle"`.

- Sample Input 1:

  ```
  3
  4
  5
  ```

  Sample Output 1:

  ```
  Right-Angled Triangle
  ```

- Sample Input 2:

  ```
  5
  5
  5
  ```

  Sample Output 2:

  ```
  Equilateral Triangle
  ```

- Sample Input 3:

  ```
  4
  4
  6
  ```

  Sample Output 3:

  ```
  Isosceles Triangle
  ```

24. **Check if a Character is Uppercase, Lowercase, Digit, or Special Character**

- Problem Statement:

  Write a program that takes a single character as input and determines its type using the following rules:
  - If the character is an uppercase letter (`A`–`Z`), print `"Uppercase Letter"`.
  - If the character is a lowercase letter (`a`–`z`), print `"Lowercase Letter"`.
  - If the character is a digit (`0`–`9`), print `"Digit"`.
  - Otherwise, print `"Special Character"`.

- Sample Input 1:

  ```
  A
  ```

  Sample Output 1:

  ```
  Uppercase Letter
  ```

- Sample Input 2:

  ```
  7
  ```

  Sample Output 2:

  ```
  Digit
  ```

- Sample Input 3:

  ```
  @
  ```

  Sample Output 3:

  ```
  Special Character
  ```

---

25. **Calculate Income Tax Based on Slabs**

- Problem Statement:

  Write a program that takes an annual income as input and calculates the total income tax using the following tax slabs:
  - Up to ₹`2,50,000` → No tax
  - ₹`2,50,001` to ₹`5,00,000` → `5%`
  - ₹`5,00,001` to ₹`10,00,000` → `20%`
  - Above ₹`10,00,000` → `30%`

  Calculate the tax cumulatively based on the applicable slabs and print the total tax amount.

- Sample Input 1:

  ```
  250000
  ```

  Sample Output 1:

  ```
  0
  ```

- Sample Input 2:

  ```
  750000
  ```

  Sample Output 2:

  ```
  62500
  ```

- Sample Input 3:

  ```
  1200000
  ```

  Sample Output 3:

  ```
  160000
  ```

26. **Print Three Numbers in Ascending Order**

- Problem Statement:

  Write a program that takes three integers as input and prints them in ascending order (smallest to largest), separated by spaces.

  Do not use any inbuilt sorting functions or methods. Use only `if-else` statements to determine the correct order.

- Sample Input 1:

  ```
  4
  2
  9
  ```

  Sample Output 1:

  ```
  2 4 9
  ```

- Sample Input 2:

  ```
  7
  7
  1
  ```

  Sample Output 2:

  ```
  1 7 7
  ```

- Sample Input 3:

  ```
  5
  3
  4
  ```

  Sample Output 3:

  ```
  3 4 5
  ```

---

27. **Print the Number of Days in a Month**

- Problem Statement:

  Write a program that takes an integer between `1` and `12` as input representing the month number and prints the number of days in that month (not considering leap years).
  - Print `31` for months: `1`, `3`, `5`, `7`, `8`, `10`, `12`
  - Print `30` for months: `4`, `6`, `9`, `11`
  - Print `28` for month: `2`
  - If the input is not between `1` and `12`, print `"Invalid month"`.

- Sample Input 1:

  ```
  4
  ```

  Sample Output 1:

  ```
  30
  ```

- Sample Input 2:

  ```
  2
  ```

  Sample Output 2:

  ```
  28
  ```

- Sample Input 3:

  ```
  13
  ```

  Sample Output 3:

  ```
  Invalid month
  ```

---

28. **Check Whether the Last Digit is Even or Odd**

- Problem Statement:

  Write a program that takes a positive integer as input and checks whether its last digit is even or odd.
  - If the last digit is even, print `"Ends with even"`.
  - Otherwise, print `"Ends with odd"`.

- Sample Input 1:

  ```
  1234
  ```

  Sample Output 1:

  ```
  Ends with even
  ```

- Sample Input 2:

  ```
  159
  ```

  Sample Output 2:

  ```
  Ends with odd
  ```

- Sample Input 3:

  ```
  820
  ```

  Sample Output 3:

  ```
  Ends with even
  ```

---

29. **Determine the Sign and Parity of a Number**

- Problem Statement:

  Write a program that takes an integer as input and prints a message based on both its sign and whether it is even or odd.
  - If it is a positive even number, print `"Positive Even"`.
  - If it is a positive odd number, print `"Positive Odd"`.
  - If it is a negative even number, print `"Negative Even"`.
  - If it is a negative odd number, print `"Negative Odd"`.
  - If it is `0`, print `"Zero"`.

- Sample Input 1:

  ```
  12
  ```

  Sample Output 1:

  ```
  Positive Even
  ```

- Sample Input 2:

  ```
  -7
  ```

  Sample Output 2:

  ```
  Negative Odd
  ```

- Sample Input 3:

  ```
  0
  ```

  Sample Output 3:

  ```
  Zero
  ```

30. **Check Whether the Tens Digit is Even or Odd**

- Problem Statement:

  Write a program that takes a positive two-digit integer as input and checks whether its tens digit is even.
  - If the tens digit is even, print `"Even Tens"`.
  - Otherwise, print `"Odd Tens"`.

- Sample Input 1:

  ```
  42
  ```

  Sample Output 1:

  ```
  Even Tens
  ```

- Sample Input 2:

  ```
  57
  ```

  Sample Output 2:

  ```
  Odd Tens
  ```

- Sample Input 3:

  ```
  86
  ```

  Sample Output 3:

  ```
  Even Tens
  ```

---

31. **Classify Age Based on Odd or Even**

- Problem Statement:

  Write a program that takes an integer input representing a person's age and prints one of the following based on its value:
  - If the age is odd and greater than or equal to `18`, print `"Odd Adult"`.
  - If the age is odd and less than `18`, print `"Odd Minor"`.
  - If the age is even, print `"Even Age"`.

- Sample Input 1:

  ```
  21
  ```

  Sample Output 1:

  ```
  Odd Adult
  ```

- Sample Input 2:

  ```
  15
  ```

  Sample Output 2:

  ```
  Odd Minor
  ```

- Sample Input 3:

  ```
  28
  ```

  Sample Output 3:

  ```
  Even Age
  ```

---

32. **Categorize a Number by Its Range**

- Problem Statement:

  Write a program that takes an integer as input and prints a label based on the following ranges:
  - If the number is less than `0`, print `"Negative"`.
  - If it is from `0` to `10` (inclusive), print `"Small"`.
  - If it is from `11` to `100` (inclusive), print `"Medium"`.
  - If it is greater than `100`, print `"Large"`.

- Sample Input 1:

  ```
  -5
  ```

  Sample Output 1:

  ```
  Negative
  ```

- Sample Input 2:

  ```
  8
  ```

  Sample Output 2:

  ```
  Small
  ```

- Sample Input 3:

  ```
  150
  ```

  Sample Output 3:

  ```
  Large
  ```
